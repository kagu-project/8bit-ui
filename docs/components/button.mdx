# Button

Use `Button` for user-triggered actions.

## Import

```tsx
import { Button, IconButton } from '@kagu-project/8bit-ui';
```

## Basic Usage

<div className="preview">
  <Button variant="solid" color="primary">
    Save
  </Button>
</div>

```tsx
<Button variant="solid" color="primary">
  Save
</Button>
```

## Button Styles

<div className="preview">
  <div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>
    <Button variant="solid" color="primary">
      Solid
    </Button>
    <Button variant="outline" color="primary">
      Outline
    </Button>
    <Button variant="link" color="primary">
      Link
    </Button>
  </div>
</div>

```tsx
<>
  <Button variant="solid" color="primary">
    Solid
  </Button>
  <Button variant="outline" color="primary">
    Outline
  </Button>
  <Button variant="link" color="primary">
    Link
  </Button>
</>
```

## Icon-Only Actions (IconButton)

Use `IconButton` for compact actions such as opening menus, closing panels, or quick utility controls.

<div className="preview">
  <div style={{ display: 'flex', gap: 12, flexWrap: 'wrap' }}>
    <IconButton aria-label="Open menu" variant="ghost" color="neutral">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M3 4h18v4H3zm0 6h18v4H3zm0 6h18v4H3z" />
      </svg>
    </IconButton>
    <IconButton aria-label="Close" variant="outline" color="primary">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M18.3 5.7a1 1 0 0 0-1.4 0L12 10.6 7.1 5.7a1 1 0 0 0-1.4 1.4l4.9 4.9-4.9 4.9a1 1 0 1 0 1.4 1.4l4.9-4.9 4.9 4.9a1 1 0 0 0 1.4-1.4l-4.9-4.9 4.9-4.9a1 1 0 0 0 0-1.4Z" />
      </svg>
    </IconButton>
    <IconButton aria-label="Delete" variant="default" color="danger">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M9 3h6l1 2h4v2H4V5h4l1-2Zm-2 6h2v9H7V9Zm4 0h2v9h-2V9Zm4 0h2v9h-2V9Z" />
      </svg>
    </IconButton>
  </div>
</div>

```tsx
<IconButton aria-label="Open menu" variant="ghost">
  <MenuIcon />
</IconButton>
```

## Button Key Props

- `variant`: `'solid' | 'outline' | 'link'`
- `color`: `'primary' | 'secondary' | 'danger' | 'neutral'`
- Plus all native `<button>` props

## IconButton Key Props

- `variant`: `'default' | 'outline' | 'ghost'`
- `color`: `'primary' | 'secondary' | 'danger' | 'neutral'`
- `size`: `'sm' | 'md' | 'lg'`
- `aria-label` is required for accessible icon-only controls

## Accessibility

- `Button` renders a native `<button>` element.
- Always provide clear text or `aria-label` for icon-only buttons.
- Keep icons `aria-hidden="true"` to avoid duplicate announcements.
