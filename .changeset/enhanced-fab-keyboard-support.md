---
"@kagu-project/8bit-ui": minor
---

Enhanced the `FAB` component with robust keyboard interaction support and refined style adjustments. Also fixed both the `Button` and `FAB` components to ensure their `type` attribute safely defaults to `'button'` and correctly handles invalid HTML types.
