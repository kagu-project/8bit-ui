---
'@kagu-project/8bit-ui': patch
---

Improved `Drawer` accessibility and runtime robustness by ensuring valid dialog naming behavior (`aria-label`/`aria-labelledby` handling), adding an SSR-safe portal guard, and documenting the naming contract.

Also tightened package quality gates by enabling strict Storybook a11y checks and raising test coverage thresholds for long-term release confidence.
