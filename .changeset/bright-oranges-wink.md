---
'@kagu-project/8bit-ui': minor
---

Added new public exports for `IconButton` and `Drawer` (including related `Drawer*` and `IconButtonProps` types), introducing new interactive primitives for consumer apps.

Also includes supporting style/usability refinements and docs examples for improved mobile behavior and component presentation.
